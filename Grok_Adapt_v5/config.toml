api_enabled = true
api_key = ""

[endpoint]
url = "https://api.x.ai/v1/responses"
model = "grok-4.3"
temperature = 0.7
max_tokens = 8048

[summarizer]
enabled = false
url = "http://localhost:8082/v1/chat/completions"
model = "summarizer"

[embeddings]
url = "http://localhost:8080/v1/chat/completions"  # or same as main if using chat for embeddings
model = "Echo"  # or a dedicated embedding model

[web_search]
url = "http://localhost:8081/search"

[paths]
home_dir = ""
context_file = ""
database = "echo_tools.db"
memory_file = "memory.md"

[prompts]
main_system = "/home/echo/grok_system_prompt.txt"
summarizer = "/home/eric/prompts/summarizer.txt"

[json_tools]
enabled = [
    "get_current_datetime",
    "web_search",
    "browse_page",
    "append_memory",
    "read_memory"
]

[context]
summarize_threshold = 180000

[security]
denylist = [
    "rm -rf",
    "rm -r /",
    "$(echo rm) -rf /tmp",
    "> /dev/",
    "dd if=/dev/zero",
    "mkfs",
    "shred",
    "wipefs",
    "fdisk",
    "parted",
    "sudo rm",
    "rm --no-preserve-root",
    ":(){ :|:& };:",
    "fork bomb",
    "> /dev/sda",
    "chmod -R 777 /",
    "chown -R",
]

